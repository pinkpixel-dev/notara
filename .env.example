# Notara reads no AI credentials from this file.
#
# The OpenAI API key is entered in Settings, under AI & Data, and stored
# encrypted by the desktop backend. A build-time variable would be readable by
# anyone with the bundle, which is exactly what this setup avoids.

VITE_APP_ENV=development
