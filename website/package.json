{
  "name": "@pinkpixel/notara-docs",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "cf:dev": "npm run build && npx wrangler pages dev ./dist --project-name notara-docs",
    "deploy": "npm run build && npx wrangler pages deploy ./dist --project-name notara-docs",
    "deploy:production": "npm run build && npx wrangler pages deploy ./dist --project-name notara-docs --branch main"
  },
  "dependencies": {
    "@astrojs/starlight": "^0.41.7",
    "astro": "^7.2.4",
    "sharp": "^0.35.3"
  }
}
